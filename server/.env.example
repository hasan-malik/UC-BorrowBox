# Server
PORT=4000
JWT_SECRET=change_me_to_a_long_random_string

# Postgres (matches docker-compose.yml)
DATABASE_URL=postgres://ucbb:ucbb_dev@localhost:5432/ucbb

# SMTP (optional in dev — if unset, OTPs are printed to the server console)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="UC BorrowBox <noreply@ucbb.local>"
