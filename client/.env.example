# Base URL of the server API.
# Leave empty in local dev — Vite proxies /api/* to localhost:4000.
# In production (Netlify), set to your Railway URL, e.g.
#   VITE_API_URL=https://ucbb-server.up.railway.app
VITE_API_URL=
